[archetype]
name = "pytorch-training-project"
version = "1.0.0"
category = "cv-ml"
tags = ["training", "pytorch", "lightning", "deep-learning"]
description = "Model training with PyTorch Lightning, Hydra config, and experiment tracking"

[skills]
required = ["pytorch-lightning", "hydra-config", "pydantic", "loguru", "testing"]
recommended = ["wandb", "model-evaluation", "data-pipelines", "docker-cv", "github-actions"]
