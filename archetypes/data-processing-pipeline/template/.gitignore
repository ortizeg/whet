# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/

# Environments
.venv/
venv/
.pixi/
pixi.lock

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Data — bulk data never lives in Git; only the fingerprinted dataset card does.
# (A directory cannot be un-ignored from within, hence the parent-by-parent form.)
/data/*
!/data/processed/
/data/processed/*
!/data/processed/dataset.json

# Reports
reports/

# OS / IDE
.DS_Store
.vscode/
.idea/
