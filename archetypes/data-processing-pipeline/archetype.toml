[archetype]
name = "data-processing-pipeline"
version = "1.0.0"
category = "cv-ml"
tags = ["data", "etl", "pipeline", "processing"]
description = "ETL workflows for dataset processing and preparation"

[skills]
required = ["pydantic", "loguru", "testing", "dvc"]
recommended = ["docker-cv", "gcp", "github-actions"]
