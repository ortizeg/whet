[skill]
name = "gradio"
version = "1.0.0"
category = "infra"
tier = "extra"
tags = ["demo", "ui", "gradio", "prototyping", "serving"]

[dependencies]
requires = ["pydantic", "loguru"]
recommends = ["huggingface", "fastapi"]

[compatibility]
python = ">=3.11"

[compatibility.libraries]
gradio = ">=4.0"
