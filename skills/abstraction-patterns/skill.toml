[skill]
name = "abstraction-patterns"
version = "1.0.0"
category = "core"
tags = ["design-patterns", "abc", "protocol", "architecture"]

[dependencies]
requires = ["pydantic"]
recommends = ["master-skill"]

[compatibility]
python = ">=3.11"
libraries = {}
