[skill]
name = "master-skill"
version = "1.0.0"
category = "core"
tags = ["conventions", "python", "coding-standards"]

[dependencies]
requires = []
recommends = ["pydantic", "code-quality", "loguru"]

[compatibility]
python = ">=3.11"
libraries = {}
