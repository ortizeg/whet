[skill]
name = "data-pipelines"
version = "1.0.0"
category = "cv-ml"
tags = ["data", "etl", "dataset", "splitting", "storage", "data-quality"]

[dependencies]
requires = []
recommends = ["dvc", "pydantic", "model-evaluation"]

[compatibility]
python = ">=3.11"
libraries = {}
