[skill]
name = "model-evaluation"
version = "1.1.0"
category = "cv-ml"
tags = ["evaluation", "metrics", "map", "detection", "supervision", "eval"]

[dependencies]
requires = []
recommends = ["testing", "matplotlib", "wandb", "dvc"]

[compatibility]
python = ">=3.11"
libraries = { supervision = ">=0.26", torchmetrics = ">=1.4", pycocotools = ">=2.0" }
