[skill]
name = "model-evaluation"
version = "1.0.0"
category = "cv-ml"
tags = ["evaluation", "metrics", "map", "detection", "classification", "eval"]

[dependencies]
requires = []
recommends = ["testing", "matplotlib", "wandb", "dvc"]

[compatibility]
python = ">=3.11"
libraries = { torchmetrics = ">=1.4", pycocotools = ">=2.0" }
